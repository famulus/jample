.row
  .col-md-6
    %table.table
    -@all_patch_sets.each do |patch_set|
      %tr
        %td{:class => ("bg-info" if false)}
          %h4
            =link_to(patch_set.patch_set_label || patch_set.id, "set_current_patch_set/#{patch_set.id}")
